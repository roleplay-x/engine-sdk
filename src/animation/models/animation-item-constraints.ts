/**
 * Constraints for animation items
 */
export interface AnimationItemConstraints {
  /**
   * List of gender identifiers that this animation item is restricted to. Null means no gender restriction.
   */
  genders?: string[];
}
