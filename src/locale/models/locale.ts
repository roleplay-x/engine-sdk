/**
 *
 * @export
 * @interface Locale
 */
export interface Locale {
  /**
   *
   * @type {string}
   * @memberof Locale
   */
  code: string;
  /**
   *
   * @type {string}
   * @memberof Locale
   */
  name: string;
  /**
   *
   * @type {string}
   * @memberof Locale
   */
  iconUrl: string;
  /**
   *
   * @type {boolean}
   * @memberof Locale
   */
  enabled: boolean;
  /**
   *
   * @type {number}
   * @memberof Locale
   */
  order: number;
}
